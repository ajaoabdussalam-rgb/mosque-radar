# Production Backend API URL (e.g. deployed on Render / Railway)
# During local development, the Vite dev proxy handles /api without this variable
VITE_API_URL=https://your-mosque-radar-api.onrender.com/api
